# Full test suite, DSP/Kleene/contract stack, and reproduce.py checks.
# Full language inference additionally requires Ollama + qwen3:8b
# (digest prefix 500a1f067a9f); reproduce.py never invokes it.
numpy>=1.24
scipy>=1.10
pandas>=2.0
pytest>=7.4
python-dateutil>=2.8
